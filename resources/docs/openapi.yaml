openapi: 3.0.3
info:
  title: Team 4 Booking Vaksin Faskes API
  description: >-
    OpenAPI specification for vaccination and healthcare service booking.
    Includes Patient Registration module endpoints.
    API for vaccination and healthcare service booking system.
    Module 3: Queue & Appointment — booking queue numbers, attendance status,
    and real-time quota reporting.
  version: 1.0.0

servers:
  - url: http://localhost:8000
    description: Local Docker

tags:
  - name: System
    description: Base system endpoints
  - name: Patient Registration
    description: Patient registration, identity verification, and history management
  - name: Schedules
    description: Vaccination schedule management
  - name: Bookings
    description: Queue & appointment booking management
  - name: Auth
    description: Authentication placeholder endpoints

paths:
  /up:
    get:
      tags:
        - System
      summary: Health check endpoint
      description: Laravel built-in health endpoint.
      responses:
        '200':
          description: Service is healthy

  /api/v1/patients:
    get:
      tags:
        - Patient Registration
      summary: List patients
      parameters:
        - in: query
          name: search
          schema:
            type: string
          description: Search by NIK or patient name
        - in: query
          name: per_page
  # -------------------------------------------------------
  # Schedules
  # -------------------------------------------------------
  /api/v1/schedules:
    get:
      tags:
        - Schedules
      summary: List available schedules
      description: Get schedules with remaining quota. Filterable by date and health center.
      parameters:
        - name: date
          in: query
          required: false
          schema:
            type: string
            format: date
            example: '2026-04-20'
          description: Filter by date (YYYY-MM-DD)
        - name: health_center_id
          in: query
          required: false
          schema:
            type: integer
            example: 1
          description: Filter by health center ID
      responses:
        '200':
          description: Schedules retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Schedule'

  /api/v1/schedules/{id}:
    get:
      tags:
        - Schedules
      summary: Get schedule detail
      description: Get a single schedule with health center, vaccine, and quota info.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Schedule retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Schedule'
        '404':
          description: Schedule not found

  /api/v1/schedules/{id}/quota:
    get:
      tags:
        - Schedules
      summary: Real-time quota report
      description: Get real-time quota usage for a specific schedule.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Quota report retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/QuotaReport'

  # -------------------------------------------------------
  # Bookings
  # -------------------------------------------------------
  /api/v1/bookings:
    get:
      tags:
        - Bookings
      summary: List bookings
      description: >-
        List bookings filtered by schedule_id + optional status, or by patient_id.
        One of schedule_id or patient_id is required.
      parameters:
        - name: schedule_id
          in: query
          required: false
          schema:
            type: integer
            example: 1
        - name: patient_id
          in: query
          required: false
          schema:
            type: integer
            example: 1
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, confirmed, checked_in, completed, cancelled]
        - name: per_page
          in: query
          required: false
          schema:
            type: integer
            default: 15
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: Paginated patient list

          description: Bookings retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Booking'

    post:
      tags:
        - Bookings
      summary: Create a new booking
      description: >-
        Book a vaccination slot. Automatically assigns a queue number.
        Validates quota availability and prevents duplicate bookings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - schedule_id
                - patient_id
              properties:
                schedule_id:
                  type: integer
                  example: 1
                patient_id:
                  type: integer
                  example: 1
      responses:
        '201':
          description: Booking created successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Booking'
        '422':
          description: Validation error (duplicate booking, no quota, etc.)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/v1/bookings/{id}:
    get:
      tags:
        - Bookings
      summary: Get booking detail
      description: Get a single booking with schedule, health center, vaccine, and patient info.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Booking retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Booking'
        '404':
          description: Booking not found

  /api/v1/bookings/{id}/check-in:
    patch:
      tags:
        - Bookings
      summary: Check in a patient
      description: >-
        Mark a patient as checked in (attendance). Only bookings with
        status 'pending' or 'confirmed' can be checked in.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Patient checked in successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Booking'
        '422':
          description: Invalid status transition

  /api/v1/bookings/{id}/complete:
    patch:
      tags:
        - Bookings
      summary: Complete a booking
      description: >-
        Mark vaccination as completed. Only bookings with status
        'checked_in' can be completed.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Booking completed successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Booking'
        '422':
          description: Invalid status transition

  /api/v1/bookings/{id}/cancel:
    patch:
      tags:
        - Bookings
      summary: Cancel a booking
      description: >-
        Cancel a booking. Cannot cancel bookings that are already
        'completed' or 'cancelled'.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                cancellation_reason:
                  type: string
                  maxLength: 500
                  example: Patient requested cancellation
      responses:
        '200':
          description: Booking cancelled successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Booking'
        '422':
          description: Invalid status transition

  # -------------------------------------------------------
  # Auth (placeholder)
  # -------------------------------------------------------
  /api/v1/auth/login:
    post:
      tags:
        - Patient Registration
      summary: Register a new patient
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - nik
                - full_name
                - birth_date
                - gender
              properties:
                nik:
                  type: string
                  example: '3201010101010001'
                full_name:
                  type: string
                  example: Budi Santoso
                birth_date:
                  type: string
                  format: date
                gender:
                  type: string
                  enum: [male, female]
                phone_number:
                  type: string
                address:
                  type: string
      responses:
        '201':
          description: Patient registered

  /api/v1/patients/verify-identity:
    post:
      tags:
        - Patient Registration
      summary: Verify patient identity with NIK and birth date
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nik, birth_date]
              properties:
                nik:
                  type: string
                birth_date:
                  type: string
                  format: date
      responses:
        '200':
          description: Identity verified
        '422':
          description: Identity mismatch

  /api/v1/patients/{patientId}/health-histories:
    get:
      tags:
        - Patient Registration
      summary: Get health histories by patient
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
      responses:
        '200':
          description: Health history list
    post:
      tags:
        - Patient Registration
      summary: Add health history to patient
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [condition_name]
              properties:
                condition_name:
                  type: string
                diagnosed_at:
                  type: string
                  format: date
                notes:
                  type: string
      responses:
        '201':
          description: Health history created

  /api/v1/patients/{patientId}/health-histories/{historyId}:
    put:
      tags:
        - Patient Registration
      summary: Update health history by external id
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
        - in: path
          name: historyId
          required: true
          description: External health history ID (health_histories.health_history_id)
          schema:
            type: string
            example: HLT-20260414-000123
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                condition_name:
                  type: string
                diagnosed_at:
                  type: string
                  format: date
                notes:
                  type: string
      responses:
        '200':
          description: Health history updated
    delete:
      tags:
        - Patient Registration
      summary: Delete health history by external id
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
        - in: path
          name: historyId
          required: true
          description: External health history ID (health_histories.health_history_id)
          schema:
            type: string
            example: HLT-20260414-000123
      responses:
        '200':
          description: Health history deleted

  /api/v1/patients/{patientId}/vaccination-histories:
    get:
      tags:
        - Patient Registration
      summary: Get vaccination histories by patient
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
      responses:
        '200':
          description: Vaccination history list
    post:
      tags:
        - Patient Registration
      summary: Add vaccination history to patient
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [vaccine_name, dose_number, vaccinated_at]
              properties:
                vaccine_name:
                  type: string
                dose_number:
                  type: integer
                vaccinated_at:
                  type: string
                  format: date-time
                provider_name:
                  type: string
                notes:
                  type: string
      responses:
        '201':
          description: Vaccination history created

  /api/v1/patients/{patientId}/vaccination-histories/{historyId}:
    put:
      tags:
        - Patient Registration
      summary: Update vaccination history by external id
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
        - in: path
          name: historyId
          required: true
          description: External vaccination history ID (vaccination_histories.vaccination_history_id)
          schema:
            type: string
            example: VAC-20260414-000321
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                vaccine_name:
                  type: string
                dose_number:
                  type: integer
                vaccinated_at:
                  type: string
                  format: date-time
                provider_name:
                  type: string
                notes:
                  type: string
      responses:
        '200':
          description: Vaccination history updated
    delete:
      tags:
        - Patient Registration
      summary: Delete vaccination history by external id
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
        - in: path
          name: historyId
          required: true
          description: External vaccination history ID (vaccination_histories.vaccination_history_id)
          schema:
            type: string
            example: VAC-20260414-000321
      responses:
        '200':
          description: Vaccination history deleted

  /api/v1/patients/{patientId}:
    get:
      tags:
        - Patient Registration
      summary: Get patient detail
      parameters:
        - in: path
          name: patientId
          required: true
          description: External patient ID (patients.patient_id)
          schema:
            type: string
            example: PAT-20260414-000001
      responses:
        '200':
          description: Patient detail
        '404':
          description: Not found

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    ApiResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        message:
          type: string
          example: Request processed.
        data:
          nullable: true
          description: Response payload.

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: Validation failed.
        errors:
          type: object
          additionalProperties:
            type: array
            items:
              type: string

    Schedule:
      type: object
      properties:
        id:
          type: integer
          example: 1
        health_center_id:
          type: integer
          example: 1
        vaccine_id:
          type: integer
          example: 1
        date:
          type: string
          format: date
          example: '2026-04-20'
        start_time:
          type: string
          example: '08:00:00'
        end_time:
          type: string
          example: '11:00:00'
        quota:
          type: integer
          example: 100
        booked_count:
          type: integer
          example: 45
          description: Number of active (non-cancelled) bookings
        health_center:
          $ref: '#/components/schemas/HealthCenter'
        vaccine:
          $ref: '#/components/schemas/Vaccine'

    HealthCenter:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: Puskesmas Kecamatan Menteng
        address:
          type: string
          example: Jl. Menteng Raya No. 12
        phone:
          type: string
          example: '021-12345678'

    Vaccine:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: Sinovac

    Patient:
      type: object
      properties:
        id:
          type: integer
          example: 1
        nik:
          type: string
          example: '3201234567890123'
        name:
          type: string
          example: Budi Santoso
        birth_date:
          type: string
          format: date
          example: '1990-05-15'
        gender:
          type: string
          enum: [male, female]
        phone:
          type: string
          example: '081234567890'
        address:
          type: string

    Booking:
      type: object
      properties:
        id:
          type: integer
          example: 1
        schedule_id:
          type: integer
          example: 1
        patient_id:
          type: integer
          example: 1
        queue_number:
          type: integer
          example: 42
        status:
          type: string
          enum: [pending, confirmed, checked_in, completed, cancelled]
          example: pending
        booked_at:
          type: string
          format: date-time
        checked_in_at:
          type: string
          format: date-time
          nullable: true
        completed_at:
          type: string
          format: date-time
          nullable: true
        cancelled_at:
          type: string
          format: date-time
          nullable: true
        cancellation_reason:
          type: string
          nullable: true
        schedule:
          $ref: '#/components/schemas/Schedule'
        patient:
          $ref: '#/components/schemas/Patient'

    QuotaReport:
      type: object
      properties:
        schedule_id:
          type: integer
          example: 1
        total:
          type: integer
          example: 100
          description: Total quota for the schedule
        used:
          type: integer
          example: 45
          description: Number of active bookings
        available:
          type: integer
          example: 55
          description: Remaining available slots
